---
import { routes } from "~/server/contants";
---
<div class="hero min-h-[90vh] bg-base-200 relative overflow-hidden">
  <!-- Decorative background elements -->
  <div class="absolute inset-0 pointer-events-none">
    <div class="absolute -top-20 -right-20 w-96 h-96 bg-primary/20 rounded-full blur-3xl animate-pulse"></div>
    <div class="absolute top-1/2 -left-20 w-72 h-72 bg-secondary/20 rounded-full blur-3xl animate-pulse delay-1000"></div>
  </div>

  <div class="hero-content text-center z-10">
    <div class="max-w-4xl">
      <div class="badge badge-primary badge-outline mb-6 gap-2 p-4">
        <span class="animate-pulse">✨</span> The future of task management
      </div>
      
      <h1 class="text-5xl md:text-7xl font-extrabold tracking-tight mb-8 leading-tight">
        Kanban that <span class="bg-gradient-to-r from-primary via-secondary to-accent bg-clip-text text-transparent">actually scales</span>
      </h1>
      
      <p class="py-6 text-xl text-base-content/70 max-w-2xl mx-auto leading-relaxed">
        Stop fighting your tools. Start shipping faster. Aboard makes task management so simple, your team will actually use it.
      </p>
      
      <div class="flex flex-col sm:flex-row gap-4 justify-center mt-8">
         <a href={routes.public.login} class="btn btn-primary btn-lg shadow-lg hover:shadow-primary/50 transition-all duration-300">
           Get Started Free
         </a>
         <a href={routes.public.login} class="btn btn-ghost btn-lg gap-2 group">
           Watch Demo
           <span class="group-hover:translate-x-1 transition-transform">→</span>
         </a>
       </div>

      <div class="mt-12 text-sm text-base-content/50">
        No credit card required · Free for small teams
      </div>
    </div>
  </div>
</div>
